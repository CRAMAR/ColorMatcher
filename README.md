# ColorMatcher

Simple cross-platform color matching web app using .NET and ImageSharp.

Quick start (requires dotnet SDK):

```bash
cd ColorMatcher
dotnet restore
dotnet run
```

Open http://localhost:5000 in your browser and upload an image.

CI: A GitHub Actions workflow is included at `.github/workflows/ci.yml` to build and publish artifacts for Linux and Windows.
